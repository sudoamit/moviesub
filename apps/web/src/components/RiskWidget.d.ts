import React from 'react';
import { ISignalSetup } from '@quant/shared';
interface RiskWidgetProps {
    selectedSignal: ISignalSetup | null;
}
export declare const RiskWidget: React.FC<RiskWidgetProps>;
export {};
