/**
 * Calculates Simple Moving Average (SMA)
 * @param values Array of numbers (e.g. close prices)
 * @param period Lookback period
 */
export declare function calculateSMA(values: number[], period: number): (number | null)[];
